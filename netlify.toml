[build]
  command = "pnpm --filter @workspace/lai-design run build:pages"
  publish = "artifacts/lai-design/dist/public"

[build.environment]
  NODE_VERSION = "20"
  VITE_BASE = "/"

# SPA routing — serve index.html for all routes
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
